<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".CasetaApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- All off in the quick settings shade (AllOffTile): acts from the lock screen too, since off is always safe -->
        <service
            android:name=".AllOffTile"
            android:exported="true"
            android:icon="@drawable/ic_power"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data android:name="android.service.quicksettings.TOGGLEABLE_TILE" android:value="false" />
        </service>

        <!-- The ten widgets (Widgets.java draws them all; HouseWidget keeps its name so a placed one stays placed) -->
        <receiver android:name=".RoomWidget" android:exported="false" android:label="@string/wg_room">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/wgi_room" />
        </receiver>
        <receiver android:name=".LightWidget" android:exported="false" android:label="@string/wg_light">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/wgi_light" />
        </receiver>
        <receiver android:name=".ScenesWidget" android:exported="false" android:label="@string/wg_scenes">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/wgi_scenes" />
        </receiver>
        <receiver android:name=".HouseWidget" android:exported="false" android:label="@string/wg_house">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/house_widget_info" />
        </receiver>
        <receiver android:name=".LevelsWidget" android:exported="false" android:label="@string/wg_levels">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/wgi_levels" />
        </receiver>
        <receiver android:name=".TimerWidget" android:exported="false" android:label="@string/wg_timer">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/wgi_timer" />
        </receiver>
        <receiver android:name=".NightstandWidget" android:exported="false" android:label="@string/wg_nightstand">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/wgi_nightstand" />
        </receiver>
        <receiver android:name=".RoutineWidget" android:exported="false" android:label="@string/wg_routine">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/wgi_routine" />
        </receiver>
        <receiver android:name=".PinnedWidget" android:exported="false" android:label="@string/wg_pinned">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/wgi_pinned" />
        </receiver>
        <receiver android:name=".ColourWidget" android:exported="false" android:label="@string/wg_colour">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/wgi_colour" />
        </receiver>

        <!-- A tap on a widget, acted on off the main thread -->
        <receiver android:name=".WidgetActions" android:exported="false" />
        <!-- A running timer's minute, and its end -->
        <receiver android:name=".TimerTick" android:exported="false" />
        <!-- The widgets reading the hub every 15 minutes -->
        <service
            android:name=".WidgetRefreshJob"
            android:exported="true"
            android:permission="android.permission.BIND_JOB_SERVICE" />
        <!-- A widget's setup, placed or edited later: hands over to the app's page for it -->
        <activity
            android:name=".WidgetConfigActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@style/WidgetConfigTheme">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Off now and Add 15 min on a sleep timer's notification -->
        <receiver android:name=".TimerActionReceiver" android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- the widgets' 15 minute read waits for a network; Android 14 and later refuse that job without this -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- a running sleep timer on the lock screen; Android 13 and later ask once, from the app, never on launch -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Android 16 and later: a running sleep timer as a Live Update, its countdown in the status bar (Settings,
         This phone, turns it off) -->
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
    <!-- a completed hold (All on, Goodnight) is felt as well as seen -->
    <uses-permission android:name="android.permission.VIBRATE" />
</manifest>
